{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "express app",
  "license": "ISC",
  "author": "zubstack",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build:ui": "cd ../frontend && npm run build && rsync -av --delete ./dist/ ../backend/dist/",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ." 
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.2.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.9.0",
    "eslint": "^10.0.1",
    "globals": "^17.3.0"
  }
}
